import { memo } from "react"
import styled from "styled-components"
import { Props } from "../types"

const LobbyContainer = memo(styled.div`

`)

export default function Lobby(props:Props) {

  return (
    <LobbyContainer>


    </LobbyContainer>
  )
}